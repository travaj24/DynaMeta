# DynaMeta audit v2 + roadmap review -- 2026-06-09

**Git HEAD at audit:** 665816b (post R1-R9, post audit-guard batch 7a04a37, post reliability roadmap)
**Method:** 16-unit multi-agent sweep -- 10 subsystem units (seeded with the prior round's 5
known-refuted false positives and its logged-open list, so effort went to NEW findings), plus a
dedicated INTEGRATION-SEAMS unit (the prior round's declared blind spot), TWO ROADMAP reviewers
(physics_depth + reliability, web-grounded), and 3 cross-cutting lenses. Verification was hardened
to TWO independent lenses per load-bearing finding (a code-refuter hunting exculpatory evidence + a
physics-derivation verifier re-deriving the formula/library semantics from scratch); "confirmed"
required BOTH lenses. 92 agents; 59 raised -> 33 confirmed-by-both, 5 uncertain, 21 refuted.
Load-bearing survivors were then HAND-RE-VERIFIED against the source before anything below was
accepted (the standing discipline; last round that step overturned five "P1"s).

## Headline

**No P0. No correctness defect on a normal path.** The four findings the workflow labeled P1 were
hand-re-graded: all four are REAL but are *defensive/seam* gaps (no built-in path triggers them) --
honest severity P2. The two-lens verification visibly worked: the lenses disagreed on severity for
3 of the 4 "P1"s, and 21 of 59 findings were killed before synthesis. The codebase remains GREEN on
physics correctness; this round's value is integration-seam hardening + roadmap accuracy.

## Landed this round (all verified, byte-identical for valid inputs)

| Finding | Fix | File |
|---|---|---|
| solve_sweep zip-TRUNCATION: a custom sweep-aware solver returning the wrong count silently produced a partial spectrum | length check, raise | `pipeline.py` |
| empty Sweep silently returned [] (vacuous success) | raise on empty bias_points/wavelengths | `pipeline.py` |
| eps_grid OUTPUT contract unchecked: a custom NToEpsMap returning (Nz,Ny,Nx) or a 4-D array silently transposed into wrong geometry | shape/ndim check at the transpose (exact (Nx,Ny,Nz) / (Nx,Ny,Nz,3,3)) | `core/bridge.py` |
| DrudeOptical accepted gamma < 0 (Im(eps) < 0 = GAIN under exp(-iwt), silent) | reject gamma < 0 / non-finite; gamma = 0 (collisionless idealization) stays legal | `materials/optical_model.py` |
| MatthiessenGamma.optical_dc_ratio unvalidated (negative flips the whole damping to gain) | `optical_dc_ratio > 0` in `__post_init__`; docstring "Gamma > 0" corrected to ">= 0, zero only if every channel is zero" | `materials/scattering.py` |
| SweepResults.from_rows: wavelengths within 1e-6 nm silently COLLIDE and overwrite rows | collision detection, raise | `results.py` |
| JAX x64 enable could silently not take (user imports jax first) | fail-fast dtype probe + documented caveat in `_ensure_jax_x64` | `core/backend.py` |
| IntersubbandEffect docstring: garbled line + the m0-cancellation asserted without the algebra | rewrote: chi_ij = (N q^2/(eps0 m0)) f_ij/(...), f_ij = 2 m0 w_ij |z_ij|^2/hbar -> m0 cancels to the dipole form; mass still enters via psi/E_i, must not be inserted AGAIN | `core/effects.py` |

Tests: bridge transposed/4-D rejection (in `test_bridge.py`), gamma<0-rejected/gamma=0-allowed,
dc_ratio guard, wavelength-collision (in `test_audit_guards.py`). R2/R3/R7/R8/R9 physics validations
re-run PASS; full suite green (see commit).

## Hand re-grade of the workflow's "P1"s

| Workflow label | Hand verdict |
|---|---|
| pipeline solve_sweep truncation (P1) | REAL mechanism; the only built-in `solve_sweep` (FDTDSweepOpticalSolver) returns exactly N, so custom-solver seam guard -> **P2, fixed** |
| DrudeOptical gamma guard (P1) | REAL asymmetry vs the m-guard; no normal path produces gamma<0; gamma=0 is a legitimate lossless idealization the guard must (and does) allow -> **P2, fixed** |
| MatthiessenGamma optical_dc_ratio (P1) | same family -> **P2, fixed** |
| bridge axis-order contract (P1) | REAL: a wrong-shaped custom eps_grid silently corrupted geometry (one lens proved it with a PoC); OUTPUT-contract check is well-posed (unlike the withdrawn extra_fields INPUT guard) -> **P2, fixed** |

## Roadmap review results

### docs/physics_depth_roadmap.md
- STATUS banner spot-checks PASS: R1-R9 commits exist (5921f85..044f0dc), the named validations exist
  and gate what the banner claims. One staleness found + FIXED: test count said 295, now 307.
- R10-R34 remain accurate and correctly prioritized post-R1-R9; no item was found to be silently
  implemented or invalidated by the Tier-0 work.

### docs/reliability_roadmap.md (REL1-REL10, web-checked)
- All canonical formulas verified against literature EXCEPT one refinement, FIXED: REL4's ENZ drift
  sensitivity d(lambda_ENZ)/dn = -(1/2) lambda/n implicitly assumed CONSTANT m_opt and eps_inf. The
  doc now states lambda_ENZ ~ sqrt(m/n) and the full form -(1/2)(lambda/n)(1 - dln m/dln n), noting
  the library's own KaneOpticalMass (dln m/dln n > 0) REDUCES the drift sensitivity.
- Builds-on driver claims and [NEW DRIVER] flags verified against the actual code symbols
  (ElectroThermalResult fields, ThermalTransientResult, the Drude ENZ crossing): accurate.

## Confirmed-but-logged (not fixed this round; honest severities)

- **integration**: extra_fields callable contract -- key-set stability across biases is not checked
  and the docstring does not state the per-bias dict must keep the same keys (P3 docs; a
  key-stability check would need loop-state). `NToEpsMap` orchestration-time validation in
  run_pipeline (we now validate at the bridge consumption point instead -- adequate).
- **carriers**: Gummel decoupled-solver test coverage thin (validation-only); LC `director_profile_bvp`
  / chiral variant coverage gap for one regime (P2-coverage); DD eta/g-factor still setup-time-only
  (logged last round).
- **optics**: Fdtd2dDesignProblem.spectrum has no absorptive-case oracle and still no TMM
  reduces-to-limit gate (logged last round, re-confirmed); `solver.py` two-wave fit warns (not
  raises) on high residual -- design choice, logged.
- **effects**: IntersubbandEffect oracle does not independently cross-check LINE STRENGTH (the TRK
  gate checks the f-sum, the position gate checks lambda_12; strength rides on the derivation) --
  a worthwhile future gate: integrate Im(eps_zz) over the line and compare to the analytic
  N_ij q^2 |z_ij|^2 pi/(eps0 hbar) sum.
- **pipeline-io**: cache key still omits solver identity/lift (logged P2 from round 1, re-confirmed);
  cache solve_time_s=0.0 on hit (logged; arguably intentional).
- **conventions**: one bare `np.transpose` vs the stated explicit-axes idiom (`core/layered.py:148`,
  P3); `validation/qcse_elliott_mqw.py` lacks a JAX-guard exercise; two FDTD sweep validations could
  add a phase/independent cross-check (P3).
- **uncertain (kept, flagged)**: resample_to_grid lacks an independent validation gate;
  fit_drude_params default bounds tightness; Sommerfeld caveat docstring (carrier_heating);
  Fdtd2dDesignProblem vacuum-end assumption note; extra-fields key stability test.

## Refuted this round (so they are not re-raised later)

21 findings died in two-lens verification; notable kills: a claimed Lorentz-ADE coefficient sign
error (the CN algebra is correct under exp(-iwt)); a claimed missing _S power in the R6 sink-outflux
(the 1/_S is correct: dS_phys = dS_mesh/_S^2 times _S from the mesh-coordinate gradient); a claimed
KK Maclaurin parity bug (the alternate-point sum is the documented O(h^2) method); a re-raise of the
bipolar n0 false positive (killed by the seeded context); a claimed Material.__post_init__ replace
ordering hazard (dataclasses.replace on frozen models is order-safe as written).

## Audit blind spots (critic, hand-screened)

Still unprobed after two rounds: DEVSIM global-state teardown ordering across two sequential
run_pipeline calls (needs an instrumented two-solve session); non-monotonic/duplicate
sweep.wavelengths_nm ordering contract vs from_rows (duplicates now raise via the collision guard;
unsorted order is handled by from_rows sorting -- but the per-row emission order vs solver order for
custom sweep-aware solvers relies on the documented in-order contract); solver.py fit-residual
warn-vs-raise policy; Design mutability vs cache identity (mutate-in-place after caching defeats any
fingerprint -- document Design-as-immutable); db.py optional NETWORK backend (JARVIS/MP REST) as a
reproducibility seam -- note: the critic's claim that its cache load "silently catches ALL
exceptions" is WRONG (db.py:249 catches exactly FileNotFoundError/ValueError); the bare
except at db.py:155 is the benign provenance-stash pattern.

## Verdict

GREEN on physics correctness (second consecutive round with zero surviving P0/P1 after hand
re-verification); the seam-hardening batch closes the highest-value integration gaps; both roadmaps
are now accurate as load-bearing documents. Remaining backlog is coverage/oracle depth, tracked above.
